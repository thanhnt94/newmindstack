# File: requirements.txt
# Phiên bản: 1.2
# Mô tả: Chứa danh sách các thư viện Python cần thiết để chạy ứng dụng Mindstack.
# Hướng dẫn cài đặt: Chạy lệnh 'pip install -r requirements.txt' trong môi trường ảo của bạn.

# --- Framework chính ---
Flask>=2.0
Werkzeug>=2.0 # Thư viện WSGI cốt lõi cho Flask

# --- Cơ sở dữ liệu & ORM ---
Flask-SQLAlchemy>=3.0
SQLAlchemy>=1.4

# --- Xác thực & Form ---
Flask-Login>=0.5
Flask-WTF>=1.0
WTForms>=3.0
email_validator>=2.0 # ĐÃ THÊM: Thư viện để hỗ trợ xác thực email

# --- Xử lý dữ liệu & File ---
pandas>=1.3 # Dùng để đọc và xử lý file Excel (.xlsx)
openpyxl>=3.0 # Thư viện phụ trợ cho pandas để làm việc với file .xlsx

# --- Tiện ích & Xử lý nội dung ---
bbcode>=1.0 # Dùng để chuyển đổi BBCode sang HTML trong các bài học
gTTS>=2.2 # Google Text-to-Speech, dùng để tạo file audio cho flashcard
pydub>=0.25 # Dùng để xử lý và ghép các file audio
audioop-lts>=0.2; python_version >= "3.13" # Cung cấp lại mô-đun audioop bị loại bỏ từ Python 3.13 trở lên

# --- Tích hợp AI ---
google-generativeai>=0.3 # Thư viện để tương tác với Google Gemini API

# --- Các thư viện phụ thuộc quan trọng khác ---
Jinja2>=3.0 # Engine template cho Flask
itsdangerous>=2.0 # Dùng để ký dữ liệu an toàn trong Flask
click>=8.0 # Dùng để tạo các lệnh command-line cho Flask
Flask-APScheduler>=1.12 # Dùng để lập lịch các tác vụ định kỳ (như gửi nhắc nhở Telegram)

# --- Tự động tìm kiếm và tải hình ảnh ---
ddgs>=5.3.0
requests>=2.31
