from typing import Any, Dict

class DefaultConfig:
    """Default configuration for the vocab_flashcard module."""
    
    MODULE_NAME = "Flashcards"
    URL_PREFIX = "/learn/vocab-flashcard"
    
    # Session defaults
    DEFAULT_BUTTON_COUNT = 3
    AUTO_AUTOPLAY = False
    SHOW_IMAGE_BY_DEFAULT = True
    
    SHOW_IMAGE_BY_DEFAULT = True
